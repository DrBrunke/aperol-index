# Kopiere diese Datei nach .env und trage deinen Key ein:  cp .env.example .env
# .env ist in .gitignore und wird NICHT eingecheckt.

# Optional: Google Places API-Key für bessere Restaurant-/POI-Suche.
# Leer lassen -> Suche läuft über OpenStreetMap (Photon/Nominatim).
GOOGLE_API_KEY=
